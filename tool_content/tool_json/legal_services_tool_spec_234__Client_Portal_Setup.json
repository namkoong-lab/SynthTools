{
  "tool_name": "Client Portal Setup",
  "tool_description": "Creates secure client portal access with authentication credentials, document sharing capabilities, and communication tools for case collaboration.",
  "parameters": {
    "client_name": {
      "type": "string",
      "required": true,
      "description": "Full legal name of the client"
    },
    "client_email": {
      "type": "string",
      "required": true,
      "description": "Client's email address for portal access"
    },
    "case_number": {
      "type": "string",
      "required": true,
      "description": "Unique case number for portal association"
    }
  },
  "error_messages": [
    "Invalid email format: Provide a valid email address",
    "Missing client information: Client name is required for portal setup",
    "Invalid case number: Provide a valid case number for portal association"
  ],
  "usage": "Provide client name, email address, and case number. The tool creates a secure portal with access credentials and case-specific features.",
  "output_details": {
    "portal_url": {
      "type": "string",
      "description": "URL for client portal access"
    },
    "username": {
      "type": "string",
      "description": "Generated username for client login"
    },
    "temporary_password": {
      "type": "string",
      "description": "Temporary password for initial login"
    },
    "setup_status": {
      "type": "string",
      "description": "Portal setup status (active, pending, failed)"
    }
  }
}