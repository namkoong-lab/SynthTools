{
  "tool_name": "Aspect Calculator",
  "tool_description": "Calculates angular relationships (aspects) between celestial bodies, including major and minor aspects with orb tolerances.",
  "parameters": {
    "planetary_positions": {
      "type": "array",
      "required": true,
      "description": "Array of planetary positions in degrees",
      "items": {
        "type": "number"
      },
      "minItems": 2,
      "maxItems": 15
    },
    "planet_names": {
      "type": "array",
      "required": true,
      "description": "Array of planet/point names corresponding to positions",
      "items": {
        "type": "string"
      },
      "minItems": 2,
      "maxItems": 15
    },
    "major_aspects_only": {
      "type": "boolean",
      "required": false,
      "description": "Calculate only major aspects (conjunction, opposition, trine, square, sextile)",
      "default": true
    },
    "orb_tolerance": {
      "type": "number",
      "required": false,
      "description": "Orb tolerance in degrees (1-10)",
      "default": 8
    },
    "include_minor_aspects": {
      "type": "boolean",
      "required": false,
      "description": "Include minor aspects (semi-sextile, quincunx, semi-square, sesquiquadrate)",
      "default": false
    },
    "separating_applying": {
      "type": "boolean",
      "required": false,
      "description": "Distinguish between separating and applying aspects",
      "default": false
    }
  },
  "error_messages": [
    "Mismatched arrays: planetary_positions and planet_names arrays must have the same length.",
    "Invalid orb tolerance: Orb tolerance must be between 1 and 10 degrees.",
    "Invalid position: All planetary positions must be between 0 and 360 degrees.",
    "Insufficient data: At least 2 planetary positions required for aspect calculation.",
    "Invalid planet names: Planet names cannot be empty strings."
  ],
  "usage": "Provide planetary_positions array with corresponding planet_names. Optionally specify aspect types, orb_tolerance, and analysis preferences. Returns detailed aspect analysis.",
  "output_details": {
    "aspects_found": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Array of aspect descriptions (e.g., 'Sun conjunct Moon 2°15')"
    },
    "aspect_count": {
      "type": "integer",
      "description": "Total number of aspects found"
    },
    "exact_aspects": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Aspects within 1 degree orb"
    },
    "strongest_aspect": {
      "type": "string",
      "description": "Aspect with smallest orb"
    }
  }
}