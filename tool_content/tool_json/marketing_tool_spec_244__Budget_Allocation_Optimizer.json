{
  "tool_name": "Budget Allocation Optimizer",
  "tool_description": "Optimizes marketing budget allocation across content channels, content types, and campaigns based on historical performance, audience reach, and strategic priorities.",
  "parameters": {
    "total_budget": {
      "type": "number",
      "required": true,
      "description": "Total content marketing budget to allocate"
    },
    "channels_performance_data": {
      "type": "array",
      "required": true,
      "description": "Historical performance data for each channel",
      "items": {
        "type": "string"
      },
      "minItems": 2,
      "maxItems": 20
    },
    "strategic_priorities": {
      "type": "array",
      "required": true,
      "description": "Strategic priorities and weights (awareness, conversion, retention)",
      "items": {
        "type": "string"
      },
      "minItems": 1,
      "maxItems": 5
    },
    "minimum_channel_budget": {
      "type": "number",
      "required": false,
      "description": "Minimum budget allocation per channel",
      "default": 500
    },
    "risk_tolerance": {
      "type": "string",
      "required": false,
      "description": "Risk tolerance level (conservative, moderate, aggressive)",
      "default": "moderate"
    }
  },
  "error_messages": [
    "Invalid budget amount: Total budget must be greater than 0.",
    "Insufficient performance data: Provide performance data for at least 2 channels.",
    "Budget too low: Total budget must exceed minimum_channel_budget × number of channels.",
    "Invalid risk tolerance: Choose from conservative, moderate, or aggressive.",
    "Missing strategic priorities: Include at least one strategic priority."
  ],
  "usage": "Specify total_budget, channels_performance_data, and strategic_priorities. Optionally set minimum_channel_budget and risk_tolerance. Returns optimized budget allocation recommendations.",
  "output_details": {
    "allocation_summary": {
      "type": "string",
      "description": "Overall budget allocation strategy summary"
    },
    "channel_allocations": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Budget allocation per channel with amounts and percentages"
    },
    "expected_outcomes": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Expected performance outcomes from allocation"
    },
    "reallocation_recommendations": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Recommendations for budget reallocation based on performance"
    }
  }
}