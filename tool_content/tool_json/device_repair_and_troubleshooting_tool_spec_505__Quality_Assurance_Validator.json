{
  "tool_name": "Quality Assurance Validator",
  "tool_description": "Performs comprehensive quality assurance validation of battery replacement including performance metrics, safety checks, and functionality verification.",
  "parameters": {
    "device_model": {
      "type": "string",
      "required": true,
      "description": "Device model that underwent battery replacement"
    },
    "test_battery_voltage": {
      "type": "number",
      "required": true,
      "description": "Measured battery voltage after installation"
    },
    "charge_test_duration": {
      "type": "integer",
      "required": true,
      "description": "Duration of charge test in minutes"
    },
    "discharge_test_duration": {
      "type": "integer",
      "required": true,
      "description": "Duration of discharge test in minutes"
    },
    "temperature_readings": {
      "type": "array",
      "required": true,
      "description": "Temperature readings during testing in Celsius",
      "items": {
        "type": "number"
      },
      "minItems": 1
    },
    "charging_current": {
      "type": "number",
      "required": true,
      "description": "Charging current in mA"
    },
    "functional_tests_passed": {
      "type": "array",
      "required": true,
      "description": "List of functional tests that passed",
      "items": {
        "type": "string"
      },
      "minItems": 1
    },
    "visual_inspection_passed": {
      "type": "boolean",
      "required": true,
      "description": "Whether visual inspection passed"
    },
    "safety_checks_passed": {
      "type": "boolean",
      "required": true,
      "description": "Whether all safety checks passed"
    },
    "customer_acceptance_criteria": {
      "type": "array",
      "required": false,
      "description": "Specific customer acceptance criteria",
      "items": {
        "type": "string"
      },
      "default": "None"
    }
  },
  "error_messages": [
    "Invalid device model: Provide a valid device model for quality standards.",
    "Voltage out of acceptable range: Battery voltage must be within 3.0-4.5V for mobile devices.",
    "Test duration insufficient: Charge and discharge tests must be at least 10 minutes each.",
    "Temperature readings required: At least one temperature reading must be provided.",
    "Excessive temperature detected: Temperature readings exceed safe operating limits (>45°C).",
    "No functional tests specified: At least one functional test result must be provided.",
    "Critical safety failure: Device failed safety checks and cannot be returned to service.",
    "Charging current out of range: Charging current must be appropriate for battery capacity."
  ],
  "usage": "Input all test results including voltage, timing, temperature, and functional test outcomes. The tool validates against quality standards and safety requirements.",
  "output_details": {
    "overall_pass_status": {
      "type": "boolean",
      "description": "Whether repair passed all quality assurance checks"
    },
    "quality_score": {
      "type": "number",
      "description": "Overall quality score out of 100"
    },
    "failed_criteria": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of quality criteria that failed"
    },
    "safety_compliance": {
      "type": "boolean",
      "description": "Whether repair meets safety compliance standards"
    },
    "performance_rating": {
      "type": "string",
      "description": "Performance rating: excellent, good, acceptable, poor"
    },
    "warranty_eligible": {
      "type": "boolean",
      "description": "Whether repair qualifies for warranty coverage"
    },
    "recommendations": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Recommendations for improvement or follow-up"
    }
  }
}