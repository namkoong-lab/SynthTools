{
  "tool_name": "Chart Canvas Generator",
  "tool_description": "Creates the base chart structure with outer ring, inner rings, and coordinate system setup for natal chart visualization.",
  "parameters": {
    "canvas_width": {
      "type": "integer",
      "required": false,
      "description": "Canvas width in pixels (300-1200)",
      "default": 600
    },
    "canvas_height": {
      "type": "integer",
      "required": false,
      "description": "Canvas height in pixels (300-1200)",
      "default": 600
    },
    "background_color": {
      "type": "string",
      "required": false,
      "description": "Background color in hex format (e.g., #FFFFFF)",
      "default": "#FFFFFF"
    },
    "line_color": {
      "type": "string",
      "required": false,
      "description": "Line color for chart elements in hex format",
      "default": "#000000"
    },
    "show_degree_marks": {
      "type": "boolean",
      "required": false,
      "description": "Whether to show degree markings around the chart",
      "default": true
    }
  },
  "error_messages": [
    "Invalid canvas dimensions: Width and height must be between 300 and 1200 pixels.",
    "Invalid color format: Use hex color format (e.g., #FFFFFF or #000000).",
    "Canvas too small: Minimum recommended size is 300x300 pixels for readable charts."
  ],
  "usage": "Set canvas dimensions and color preferences. Creates the foundational chart structure ready for adding planetary positions, houses, and aspects.",
  "output_details": {
    "canvas_spec": {
      "type": "string",
      "description": "Canvas specification string with dimensions and settings"
    },
    "center_x": {
      "type": "number",
      "description": "X coordinate of chart center"
    },
    "center_y": {
      "type": "number",
      "description": "Y coordinate of chart center"
    },
    "outer_radius": {
      "type": "number",
      "description": "Outer radius of chart wheel"
    },
    "inner_radius": {
      "type": "number",
      "description": "Inner radius for planet placement"
    }
  }
}