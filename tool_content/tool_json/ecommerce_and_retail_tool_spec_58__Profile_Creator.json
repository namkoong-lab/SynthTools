{
  "tool_name": "Profile Creator",
  "tool_description": "Creates a new customer profile with validated data, assigns unique identifiers, and initializes default privacy settings.",
  "parameters": {
    "validated_data": {
      "type": "string",
      "required": true,
      "description": "JSON string of validated profile data from Profile Data Validator"
    },
    "account_type": {
      "type": "string",
      "required": false,
      "description": "Account type: standard, premium, or business",
      "default": "standard"
    },
    "marketing_opt_in": {
      "type": "boolean",
      "required": false,
      "description": "Initial marketing communication consent",
      "default": false
    }
  },
  "error_messages": [
    "Invalid validated_data format: Provide properly formatted JSON string from Profile Data Validator.",
    "Duplicate email detected: This email address is already registered in the system.",
    "Invalid account_type: Use one of [standard, premium, business].",
    "Profile creation failed: Unable to create profile due to system constraints."
  ],
  "usage": "Use validated data from Profile Data Validator to create a new customer profile. The tool generates unique customer ID and sets up initial account structure.",
  "output_details": {
    "customer_id": {
      "type": "string",
      "description": "Unique identifier assigned to the new customer profile"
    },
    "profile_created": {
      "type": "boolean",
      "description": "Whether profile creation was successful"
    },
    "creation_timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "When the profile was created"
    }
  }
}