{
  "tool_name": "Uncertainty Calculator",
  "tool_description": "Calculates comprehensive measurement uncertainty budgets following GUM methodology, combining Type A and Type B uncertainty components for calibration results.",
  "parameters": {
    "type_a_uncertainty": {
      "type": "array",
      "required": true,
      "description": "Type A uncertainties from statistical analysis in micrometers",
      "items": {
        "type": "number"
      },
      "minItems": 1,
      "maxItems": 50
    },
    "reference_uncertainty": {
      "type": "array",
      "required": true,
      "description": "Reference standard uncertainties in micrometers",
      "items": {
        "type": "number"
      }
    },
    "environmental_uncertainty": {
      "type": "number",
      "required": true,
      "description": "Environmental condition uncertainty contribution in micrometers"
    },
    "resolution_uncertainty": {
      "type": "number",
      "required": true,
      "description": "Instrument resolution uncertainty in micrometers"
    },
    "thermal_uncertainty": {
      "type": "number",
      "required": false,
      "description": "Thermal expansion uncertainty in micrometers",
      "default": 0.1
    },
    "drift_uncertainty": {
      "type": "number",
      "required": false,
      "description": "Instrument drift uncertainty in micrometers",
      "default": 0.05
    },
    "coverage_factor": {
      "type": "number",
      "required": false,
      "description": "Coverage factor for expanded uncertainty (typically 2)",
      "default": 2.0
    },
    "gum_methodology": {
      "type": "boolean",
      "required": false,
      "description": "Use GUM uncertainty propagation methodology",
      "default": true
    }
  },
  "error_messages": [
    "Array length mismatch: type_a_uncertainty and reference_uncertainty must have same length.",
    "Invalid uncertainty values: All uncertainty values must be positive numbers.",
    "Coverage factor out of range: Use coverage factor between 1.0 and 3.0.",
    "Uncertainty calculation failed: Unable to compute uncertainty budget with provided inputs."
  ],
  "usage": "Provide all uncertainty components from statistical analysis and known sources. Tool combines components using GUM methodology to calculate expanded uncertainties.",
  "output_details": {
    "combined_uncertainties": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "description": "Combined standard uncertainties for each point in micrometers"
    },
    "expanded_uncertainties": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "description": "Expanded uncertainties (k=2) in micrometers"
    },
    "uncertainty_budget": {
      "type": "string",
      "description": "Detailed uncertainty budget breakdown"
    },
    "dominant_sources": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Primary uncertainty contributors identified"
    }
  }
}