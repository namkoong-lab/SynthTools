{
  "tool_name": "Optimal Speed Calculator",
  "tool_description": "Calculates optimal conveyor belt speed to achieve target throughput while considering system constraints and efficiency factors.",
  "parameters": {
    "target_throughput": {
      "type": "number",
      "required": true,
      "description": "Target throughput in kg per hour (1-100000)"
    },
    "belt_width": {
      "type": "number",
      "required": true,
      "description": "Belt width in meters (0.1-10.0)"
    },
    "material_density": {
      "type": "number",
      "required": true,
      "description": "Material density in kg per cubic meter (1-10000)"
    },
    "load_height": {
      "type": "number",
      "required": true,
      "description": "Material load height on belt in meters (0.001-1.0)"
    },
    "max_belt_speed": {
      "type": "number",
      "required": true,
      "description": "Maximum allowable belt speed in meters per minute (1-1000)"
    },
    "min_belt_speed": {
      "type": "number",
      "required": true,
      "description": "Minimum operational belt speed in meters per minute (0.1-100)"
    },
    "efficiency_factor": {
      "type": "number",
      "required": false,
      "description": "System efficiency factor (0.1-1.0)",
      "default": 0.85
    },
    "power_constraint": {
      "type": "number",
      "required": false,
      "description": "Maximum power consumption in kW (1-1000)",
      "default": null
    }
  },
  "error_messages": [
    "Invalid throughput target: target_throughput must be between 1 and 100000 kg/hour.",
    "Invalid belt specifications: Verify belt_width (0.1-10.0m), material_density (1-10000 kg/m³), and load_height (0.001-1.0m).",
    "Speed constraint conflict: min_belt_speed must be less than max_belt_speed.",
    "Impossible throughput: Target throughput cannot be achieved with given constraints. Reduce target or increase belt capacity.",
    "Invalid efficiency factor: efficiency_factor must be between 0.1 and 1.0.",
    "Invalid power constraint: power_constraint must be positive if specified."
  ],
  "usage": "Specify target throughput, belt specifications, and speed constraints. The tool calculates the optimal belt speed to achieve the target efficiently.",
  "output_details": {
    "optimal_speed": {
      "type": "number",
      "description": "Calculated optimal belt speed in meters per minute"
    },
    "achievable_throughput": {
      "type": "number",
      "description": "Throughput achievable at optimal speed in kg per hour"
    },
    "power_requirement": {
      "type": "number",
      "description": "Estimated power requirement in kW"
    },
    "efficiency_rating": {
      "type": "number",
      "description": "Operating efficiency percentage at optimal speed"
    },
    "constraint_status": {
      "type": "string",
      "description": "Status indicating if constrained by speed, power, or capacity limits"
    }
  }
}