{
  "tool_name": "Multi Location Aggregator",
  "tool_description": "Aggregates weather forecasts from multiple geographic locations to create regional summaries and identify spatial weather patterns.",
  "parameters": {
    "location_coordinates": {
      "type": "array",
      "required": true,
      "description": "Array of location coordinate pairs as strings (lat,lon)",
      "items": {
        "type": "string"
      },
      "minItems": 2,
      "maxItems": 50
    },
    "location_names": {
      "type": "array",
      "required": true,
      "description": "Corresponding location names",
      "items": {
        "type": "string"
      },
      "minItems": 2,
      "maxItems": 50
    },
    "forecast_data": {
      "type": "array",
      "required": true,
      "description": "Forecast values for all locations (flattened array)",
      "items": {
        "type": "number"
      },
      "minItems": 6,
      "maxItems": 5000
    },
    "aggregation_method": {
      "type": "string",
      "required": false,
      "description": "Method for combining forecasts: mean, median, weighted_average",
      "default": "mean"
    },
    "weight_factors": {
      "type": "array",
      "required": false,
      "description": "Weight factors for each location (for weighted_average)",
      "items": {
        "type": "number"
      },
      "default": "None"
    },
    "regional_name": {
      "type": "string",
      "required": true,
      "description": "Name for the regional forecast area"
    },
    "time_periods": {
      "type": "array",
      "required": true,
      "description": "Time periods for the forecasts",
      "items": {
        "type": "string"
      },
      "minItems": 1,
      "maxItems": 240
    },
    "include_spatial_analysis": {
      "type": "boolean",
      "required": false,
      "description": "Include spatial gradient and pattern analysis",
      "default": false
    }
  },
  "error_messages": [
    "Array length mismatch: location_coordinates and location_names must have the same length.",
    "Forecast data insufficient: forecast_data length must match number of locations × time periods.",
    "Invalid aggregation method: Use one of [mean, median, weighted_average].",
    "Weight factors mismatch: If using weighted_average, weight_factors must have same length as locations.",
    "Invalid coordinates format: Use 'latitude,longitude' format for coordinates.",
    "Time period mismatch: Number of time_periods must match forecast data structure."
  ],
  "usage": "Provide arrays of location coordinates, names, and corresponding forecast data. Specify regional_name and time_periods. Optionally choose aggregation_method and enable spatial analysis. Returns regional forecast summary.",
  "output_details": {
    "regional_forecast": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "description": "Aggregated forecast values for the region"
    },
    "forecast_range": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "description": "Min and max values across all locations"
    },
    "spatial_patterns": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Identified spatial weather patterns"
    },
    "location_rankings": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Locations ranked by forecast severity"
    },
    "confidence_level": {
      "type": "number",
      "description": "Regional forecast confidence (0-100)"
    }
  }
}