{
  "tool_name": "Price Change Calculator",
  "tool_description": "Calculates required price changes and impact analysis for synchronizing prices across channels, including cost and revenue projections.",
  "parameters": {
    "current_prices": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "required": true,
      "description": "Current prices across channels"
    },
    "target_prices": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "required": true,
      "description": "Desired target prices"
    },
    "sales_volumes": {
      "type": "array",
      "items": {
        "type": "integer"
      },
      "required": true,
      "description": "Historical sales volumes for impact calculation"
    }
  },
  "error_messages": [
    "Array length mismatch: current_prices, target_prices, and sales_volumes must have equal length.",
    "Invalid price data: Prices must be positive numbers.",
    "Invalid volume data: Sales volumes must be non-negative integers."
  ],
  "usage": "Provide current_prices, target_prices, and sales_volumes arrays to calculate price changes and projected business impact.",
  "output_details": {
    "price_deltas": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "description": "Absolute price changes required"
    },
    "percentage_changes": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "description": "Percentage changes for each price"
    },
    "revenue_impact": {
      "type": "number",
      "description": "Projected revenue impact of price changes"
    },
    "total_changes": {
      "type": "integer",
      "description": "Number of prices requiring changes"
    }
  }
}