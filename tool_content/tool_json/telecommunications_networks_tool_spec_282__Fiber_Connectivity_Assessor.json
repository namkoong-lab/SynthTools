{
  "tool_name": "Fiber Connectivity Assessor",
  "tool_description": "Evaluates fiber optic backhaul connectivity options for proposed network sites including existing fiber infrastructure and deployment requirements.",
  "parameters": {
    "latitude": {
      "type": "number",
      "required": true,
      "description": "Site latitude in decimal degrees"
    },
    "longitude": {
      "type": "number",
      "required": true,
      "description": "Site longitude in decimal degrees"
    },
    "bandwidth_requirements": {
      "type": "number",
      "required": true,
      "description": "Required bandwidth in Gbps"
    },
    "search_radius_km": {
      "type": "number",
      "required": false,
      "description": "Search radius for fiber infrastructure in kilometers",
      "default": 5
    }
  },
  "error_messages": [
    "Invalid coordinates: Latitude must be between -90 and 90, longitude between -180 and 180.",
    "Invalid bandwidth requirements: Must be a positive number.",
    "Invalid search radius: Radius must be between 0.1 and 20 kilometers.",
    "Fiber infrastructure data unavailable: Unable to access fiber network database.",
    "Connectivity assessment failed: Unable to complete fiber connectivity analysis."
  ],
  "usage": "Provide site coordinates and bandwidth requirements. The tool evaluates available fiber options and connectivity feasibility within the search radius.",
  "output_details": {
    "fiber_available": {
      "type": "boolean",
      "description": "Whether fiber connectivity is available"
    },
    "distance_to_fiber_m": {
      "type": "number",
      "description": "Distance to nearest fiber connection in meters"
    },
    "available_bandwidth": {
      "type": "number",
      "description": "Available bandwidth capacity in Gbps"
    },
    "fiber_providers": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of available fiber service providers"
    },
    "connectivity_options": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Available connectivity options (fiber, microwave, satellite)"
    }
  }
}