{
  "tool_name": "Photography Setup Validator",
  "tool_description": "Verifies and calibrates photography equipment settings to ensure consistent, high-quality documentation according to museum standards.",
  "parameters": {
    "camera_model": {
      "type": "string",
      "required": true,
      "description": "Camera model being used for documentation"
    },
    "lighting_type": {
      "type": "string",
      "required": true,
      "description": "Type of lighting setup: led_panel, tungsten, strobe, natural, mixed"
    },
    "color_temperature": {
      "type": "integer",
      "required": true,
      "description": "Color temperature in Kelvin (2000-10000)"
    },
    "background_type": {
      "type": "string",
      "required": true,
      "description": "Background material: neutral_gray, white, black, gradient, custom"
    }
  },
  "error_messages": [
    "Unsupported camera model: Camera not in approved equipment list",
    "Invalid lighting type: Must be one of [led_panel, tungsten, strobe, natural, mixed]",
    "Color temperature out of range: Must be between 2000-10000 Kelvin",
    "Invalid background type: Must be one of [neutral_gray, white, black, gradient, custom]",
    "Equipment calibration expired: Camera or lighting requires recalibration"
  ],
  "usage": "Run before each photography session to ensure equipment is properly configured and calibrated for museum documentation standards.",
  "output_details": {
    "validation_status": {
      "type": "boolean",
      "description": "Whether setup meets museum photography standards"
    },
    "calibration_date": {
      "type": "string",
      "description": "Last calibration date of equipment"
    },
    "recommended_settings": {
      "type": "string",
      "description": "Optimal camera settings for current setup"
    },
    "setup_notes": {
      "type": "string",
      "description": "Additional setup recommendations and warnings"
    }
  }
}