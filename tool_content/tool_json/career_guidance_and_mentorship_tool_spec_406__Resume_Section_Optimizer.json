{
  "tool_name": "Resume Section Optimizer",
  "tool_description": "Optimizes individual resume sections (summary, experience, skills, education) based on job requirements and best practices.",
  "parameters": {
    "section_content": {
      "type": "string",
      "required": true,
      "description": "Current content of the resume section to optimize"
    },
    "section_type": {
      "type": "string",
      "required": true,
      "description": "Type of section (summary, experience, skills, education, projects)"
    },
    "job_requirements": {
      "type": "array",
      "required": true,
      "description": "Key requirements from target job posting",
      "items": {
        "type": "string"
      },
      "minItems": 1,
      "maxItems": 20
    },
    "target_role": {
      "type": "string",
      "required": true,
      "description": "Specific job title being applied for"
    },
    "years_experience": {
      "type": "integer",
      "required": false,
      "description": "Applicant's years of relevant experience",
      "default": 5
    },
    "industry": {
      "type": "string",
      "required": false,
      "description": "Target industry for role-specific optimization",
      "default": "None"
    },
    "word_limit": {
      "type": "integer",
      "required": false,
      "description": "Maximum words for the optimized section",
      "default": 150
    }
  },
  "error_messages": [
    "Invalid section type: Use one of [summary, experience, skills, education, projects].",
    "Empty content: Provide existing section content to optimize.",
    "Invalid word limit: Word limit must be between 20 and 500.",
    "Insufficient job requirements: Provide at least one job requirement for targeted optimization."
  ],
  "usage": "Input a specific resume section with job requirements to receive optimized content that better aligns with the target role and improves impact.",
  "output_details": {
    "optimized_content": {
      "type": "string",
      "description": "Enhanced section content tailored to job requirements"
    },
    "improvement_summary": {
      "type": "string",
      "description": "Explanation of key changes and improvements made"
    },
    "keyword_integration": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Job-relevant keywords successfully incorporated into the section"
    },
    "word_count": {
      "type": "integer",
      "description": "Final word count of optimized section"
    },
    "impact_score": {
      "type": "number",
      "description": "Estimated improvement in section effectiveness (0-100)"
    }
  }
}