{
  "tool_name": "Permeability Calculator",
  "tool_description": "Calculates hydraulic conductivity from constant head, falling head, and flexible wall permeameter test data.",
  "parameters": {
    "sample_ids": {
      "type": "array",
      "required": true,
      "items": {
        "type": "string"
      },
      "description": "List of sample IDs with permeability test data"
    },
    "test_method": {
      "type": "string",
      "required": true,
      "description": "Test method: constant_head, falling_head, or flexible_wall"
    },
    "water_temperature": {
      "type": "number",
      "required": true,
      "description": "Water temperature during test in Celsius"
    }
  },
  "error_messages": [
    "Invalid sample IDs: One or more samples do not have permeability test data.",
    "Unsupported test method: Use one of [constant_head, falling_head, flexible_wall].",
    "Invalid water temperature: Must be between 5 and 40 degrees Celsius.",
    "Incomplete test data: Samples must have all required measurements for the specified test method."
  ],
  "usage": "Specify sample_ids, test_method, and water_temperature to calculate hydraulic conductivity values with temperature corrections.",
  "output_details": {
    "hydraulic_conductivity": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "description": "Hydraulic conductivity values in m/s"
    },
    "permeability_classification": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Permeability classification (high, medium, low, very low)"
    },
    "temperature_corrected": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "description": "Temperature-corrected hydraulic conductivity at 20°C"
    }
  }
}