{
  "tool_name": "Metadata Schema Validator",
  "tool_description": "Validates metadata fields against museum standards and cataloging rules to ensure data consistency and completeness across the collection.",
  "parameters": {
    "metadata_fields": {
      "type": "array",
      "required": true,
      "description": "List of metadata field names to validate",
      "items": {
        "type": "string"
      },
      "minItems": 1
    },
    "metadata_values": {
      "type": "array",
      "required": true,
      "description": "Corresponding values for each metadata field",
      "items": {
        "type": "string"
      },
      "minItems": 1
    },
    "schema_standard": {
      "type": "string",
      "required": true,
      "description": "Metadata standard to validate against: dublin_core, cdwa_lite, vra_core, custom"
    },
    "object_type": {
      "type": "string",
      "required": true,
      "description": "Type of object for context-specific validation"
    },
    "required_only": {
      "type": "boolean",
      "required": false,
      "description": "Whether to validate only required fields",
      "default": false
    }
  },
  "error_messages": [
    "Field-value mismatch: metadata_fields and metadata_values arrays must have same length",
    "Invalid schema standard: Must be one of [dublin_core, cdwa_lite, vra_core, custom]",
    "Empty metadata arrays: Both metadata_fields and metadata_values must contain at least one item",
    "Unknown metadata field: One or more field names not recognized in specified schema",
    "Invalid field format: Field values do not conform to schema requirements",
    "Missing required fields: Required metadata fields are not present"
  ],
  "usage": "Validate metadata before database entry to ensure compliance with institutional and professional standards. Run validation multiple times during metadata development.",
  "output_details": {
    "validation_passed": {
      "type": "boolean",
      "description": "Whether all metadata passes validation"
    },
    "field_errors": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of specific validation errors found"
    },
    "completeness_score": {
      "type": "number",
      "description": "Percentage of recommended fields completed"
    },
    "missing_required": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of required fields that are missing"
    },
    "suggested_fields": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Recommended additional fields for this object type"
    }
  }
}