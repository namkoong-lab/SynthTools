{
  "tool_name": "Credit Score Calculator",
  "tool_description": "Calculates proprietary credit scores using custom scoring models and risk features, providing detailed score components and reasoning.",
  "parameters": {
    "risk_features": {
      "type": "array",
      "required": true,
      "description": "Array of engineered risk features from feature engineering tool",
      "items": {
        "type": "number"
      },
      "minItems": 10,
      "maxItems": 100
    },
    "model_type": {
      "type": "string",
      "required": false,
      "description": "Scoring model to use: logistic, gradient_boosting, neural_network",
      "default": "gradient_boosting"
    },
    "score_range": {
      "type": "string",
      "required": false,
      "description": "Output score range: FICO_range, custom_1000, percentile",
      "default": "FICO_range"
    }
  },
  "error_messages": [
    "Invalid risk features: Feature array must contain numeric values within expected ranges.",
    "Unsupported model type: Use one of [logistic, gradient_boosting, neural_network].",
    "Invalid score range: Use one of [FICO_range, custom_1000, percentile].",
    "Insufficient features: Minimum 10 features required for reliable scoring.",
    "Model execution error: Unable to generate score due to feature incompatibility."
  ],
  "usage": "Provide risk_features array from the Risk Feature Engineer tool, optionally specify model_type and score_range. Returns calculated credit score with detailed component breakdown.",
  "output_details": {
    "credit_score": {
      "type": "number",
      "description": "Calculated proprietary credit score"
    },
    "score_percentile": {
      "type": "number",
      "description": "Percentile ranking of the score (0-100)"
    },
    "score_grade": {
      "type": "string",
      "description": "Letter grade representation: A, B, C, D, F"
    },
    "component_scores": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "description": "Individual component scores that make up the total"
    },
    "model_confidence": {
      "type": "number",
      "description": "Model confidence in the score prediction (0-100)"
    }
  }
}