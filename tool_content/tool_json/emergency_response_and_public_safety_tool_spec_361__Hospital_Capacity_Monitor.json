{
  "tool_name": "Hospital Capacity Monitor",
  "tool_description": "Monitors hospital bed availability, emergency department capacity, and specialty services for patient transport decisions.",
  "parameters": {
    "patient_lat": {
      "type": "number",
      "required": true,
      "description": "Patient location latitude"
    },
    "patient_lng": {
      "type": "number",
      "required": true,
      "description": "Patient location longitude"
    },
    "specialty_needed": {
      "type": "string",
      "required": false,
      "default": "general",
      "description": "Required specialty: general, trauma, cardiac, stroke, pediatric, burn"
    },
    "patient_age": {
      "type": "integer",
      "required": true,
      "description": "Patient age in years"
    },
    "acuity_level": {
      "type": "integer",
      "required": true,
      "description": "Patient acuity level 1-5"
    },
    "insurance_type": {
      "type": "string",
      "required": false,
      "default": "unknown",
      "description": "Insurance: medicare, medicaid, private, uninsured, unknown"
    },
    "max_transport_distance": {
      "type": "number",
      "required": false,
      "default": 25,
      "description": "Maximum transport distance in miles"
    }
  },
  "error_messages": [
    "Invalid coordinates: Patient location coordinates must be valid latitude/longitude",
    "Invalid specialty type: Use general, trauma, cardiac, stroke, pediatric, or burn",
    "Invalid patient age: Age must be between 0 and 120 years",
    "Invalid acuity level: Must be integer between 1 and 5",
    "No hospitals available: No suitable hospitals found within transport distance",
    "Invalid insurance type: Use medicare, medicaid, private, uninsured, or unknown"
  ],
  "usage": "Check hospital availability and capacity for patient transport based on location, medical needs, and specialty requirements.",
  "output_details": {
    "recommended_hospitals": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of recommended hospital names in priority order"
    },
    "hospital_distances": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "description": "Distance in miles to each recommended hospital"
    },
    "bed_availability": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Bed status for each hospital: available, limited, full"
    },
    "estimated_wait_times": {
      "type": "array",
      "items": {
        "type": "integer"
      },
      "description": "Estimated ED wait times in minutes"
    }
  }
}