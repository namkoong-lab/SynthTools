{
  "tool_name": "Forecast Post Processor",
  "tool_description": "Applies bias correction, statistical downscaling, and calibration to raw numerical weather model output to improve forecast accuracy.",
  "parameters": {
    "raw_forecast_data": {
      "type": "array",
      "required": true,
      "description": "Raw forecast values from numerical model",
      "items": {
        "type": "number"
      },
      "minItems": 1,
      "maxItems": 1000
    },
    "forecast_times": {
      "type": "array",
      "required": true,
      "description": "Corresponding forecast valid times",
      "items": {
        "type": "string"
      },
      "minItems": 1,
      "maxItems": 1000
    },
    "variable_type": {
      "type": "string",
      "required": true,
      "description": "Weather variable being processed: temperature, precipitation, wind, pressure, humidity"
    },
    "bias_correction": {
      "type": "boolean",
      "required": false,
      "description": "Apply bias correction based on historical errors",
      "default": true
    },
    "downscaling_method": {
      "type": "string",
      "required": false,
      "description": "Statistical downscaling approach: linear, quantile_mapping, neural_network",
      "default": "quantile_mapping"
    }
  },
  "error_messages": [
    "Data length mismatch: raw_forecast_data and forecast_times arrays must have the same length.",
    "Invalid variable type: Use one of [temperature, precipitation, wind, pressure, humidity].",
    "Invalid downscaling method: Use one of [linear, quantile_mapping, neural_network].",
    "Insufficient data: Need at least 1 data point for processing.",
    "Processing failed: Post-processing could not be completed. Check input data quality and parameters."
  ],
  "usage": "Provide raw forecast data with corresponding times and specify the variable type. Optionally enable bias_correction and choose a downscaling_method. Returns calibrated forecast values.",
  "output_details": {
    "processed_forecast": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "description": "Bias-corrected and calibrated forecast values"
    },
    "uncertainty_bounds": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "description": "Forecast uncertainty range (±)"
    },
    "processing_summary": {
      "type": "string",
      "description": "Description of post-processing steps applied"
    },
    "improvement_score": {
      "type": "number",
      "description": "Improvement over raw forecast (percentage)"
    }
  }
}