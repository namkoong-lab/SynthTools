{
  "tool_name": "Task Dependency Mapper",
  "tool_description": "Maps dependencies and optimal sequencing between cleaning tasks to ensure logical order and efficiency.",
  "parameters": {
    "primary_task_id": {
      "type": "string",
      "required": true,
      "description": "Main task to analyze dependencies for"
    },
    "room_context": {
      "type": "string",
      "required": true,
      "description": "Room where task sequence occurs"
    },
    "dependency_type": {
      "type": "string",
      "required": true,
      "description": "Type of dependency (prerequisite, sequence, resource, timing)"
    }
  },
  "error_messages": [
    "Invalid primary_task_id: Task does not exist",
    "Invalid room_context: Room ID does not exist",
    "Invalid dependency_type: Must be one of [prerequisite, sequence, resource, timing]"
  ],
  "usage": "Use this tool to understand which tasks should be done before others for maximum efficiency and effectiveness.",
  "output_details": {
    "prerequisites": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Tasks that must be completed first"
    },
    "following_tasks": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Tasks that should follow this one"
    },
    "optimal_sequence": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Recommended order for task group"
    }
  }
}