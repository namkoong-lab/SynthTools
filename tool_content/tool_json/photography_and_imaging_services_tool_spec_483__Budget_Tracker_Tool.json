{
  "tool_name": "Budget Tracker Tool",
  "tool_description": "Tracks photography service costs, additional fees, and payment schedules while monitoring budget adherence throughout the planning process.",
  "parameters": {
    "base_package_cost": {
      "type": "number",
      "required": true,
      "description": "Base photography package cost"
    },
    "additional_services": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "required": false,
      "description": "Additional services requested",
      "default": []
    },
    "travel_distance_miles": {
      "type": "number",
      "required": false,
      "description": "Travel distance to venue in miles",
      "default": 0
    }
  },
  "error_messages": [
    "Invalid base cost: Base package cost must be greater than 0",
    "Invalid travel distance: Travel distance must be 0 or greater",
    "Negative additional costs: Additional service costs cannot be negative"
  ],
  "usage": "Provide base_package_cost and optionally specify additional_services and travel_distance_miles for comprehensive budget calculation.",
  "output_details": {
    "total_cost": {
      "type": "number",
      "description": "Total estimated cost including all services"
    },
    "cost_breakdown": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Itemized cost breakdown"
    },
    "payment_schedule": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Recommended payment schedule"
    }
  }
}