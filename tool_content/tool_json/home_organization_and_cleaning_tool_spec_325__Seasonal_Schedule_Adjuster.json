{
  "tool_name": "Seasonal Schedule Adjuster",
  "tool_description": "Modifies cleaning schedules to account for seasonal changes in cleaning needs, frequency, and specific seasonal tasks.",
  "parameters": {
    "base_schedule_id": {
      "type": "string",
      "required": true,
      "description": "Schedule to adjust for seasons"
    },
    "target_season": {
      "type": "string",
      "required": true,
      "description": "Season to optimize for (spring, summer, fall, winter)"
    },
    "climate_zone": {
      "type": "string",
      "required": false,
      "description": "Geographic climate (humid, dry, temperate, cold)",
      "default": "temperate"
    },
    "include_seasonal_tasks": {
      "type": "boolean",
      "required": false,
      "description": "Whether to add season-specific tasks",
      "default": true
    }
  },
  "error_messages": [
    "Invalid base_schedule_id: Schedule does not exist",
    "Invalid target_season: Must be one of [spring, summer, fall, winter]",
    "Invalid climate_zone: Must be one of [humid, dry, temperate, cold]"
  ],
  "usage": "Adjust your cleaning schedule for different seasons to address seasonal cleaning needs like spring cleaning or winter maintenance.",
  "output_details": {
    "adjusted_schedule_id": {
      "type": "string",
      "description": "ID of the seasonally adjusted schedule"
    },
    "seasonal_additions": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "New seasonal tasks added"
    },
    "frequency_changes": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Tasks with modified frequencies"
    }
  }
}