{
  "tool_name": "Geographic Zone Analyzer",
  "tool_description": "Analyzes service territories and job locations to optimize geographic zone assignments and minimize technician travel distances.",
  "parameters": {
    "service_area_boundary": {
      "type": "string",
      "required": true,
      "description": "Service area boundary coordinates or region name"
    },
    "job_locations": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "required": true,
      "description": "List of job site addresses or coordinates"
    },
    "technician_home_bases": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "required": true,
      "description": "Home base locations for each technician"
    },
    "zone_count": {
      "type": "integer",
      "required": false,
      "default": 5,
      "description": "Desired number of service zones"
    },
    "balancing_method": {
      "type": "string",
      "required": false,
      "default": "workload",
      "description": "Zone balancing method: workload, geographic, hybrid"
    }
  },
  "error_messages": [
    "Invalid zone count: Must be between 2 and 20",
    "Insufficient locations: Need at least 10 job locations for meaningful analysis",
    "Invalid balancing method: Must be one of [workload, geographic, hybrid]",
    "Invalid coordinates: Provide valid addresses or GPS coordinates"
  ],
  "usage": "Provide service area boundaries, job locations, and technician bases to receive optimized zone assignments that minimize travel and balance workload.",
  "output_details": {
    "zone_assignments": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Zone assignments in format 'zone_id:job_location:assigned_technician'"
    },
    "zone_centroids": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Geographic center points for each zone"
    },
    "travel_optimization_score": {
      "type": "number",
      "description": "Travel distance improvement score (0-100)"
    },
    "zone_balance_score": {
      "type": "number",
      "description": "Workload balance score across zones (0-100)"
    }
  }
}