{
  "tool_name": "Contract Template Generator",
  "tool_description": "Generates customized photography service contracts based on client requirements, event details, and service specifications.",
  "parameters": {
    "client_name": {
      "type": "string",
      "required": true,
      "description": "Client name for contract"
    },
    "event_date": {
      "type": "string",
      "required": true,
      "description": "Event date",
      "format": "date-time"
    },
    "service_package": {
      "type": "string",
      "required": true,
      "description": "Photography service package selected"
    },
    "total_cost": {
      "type": "number",
      "required": true,
      "description": "Total contract value"
    }
  },
  "error_messages": [
    "Empty client name: Provide valid client name for contract generation",
    "Invalid event date: Provide valid future event date",
    "Invalid service package: Specify valid service package",
    "Invalid total cost: Total cost must be greater than 0"
  ],
  "usage": "Provide client_name, event_date, service_package, and total_cost to generate a customized photography service contract.",
  "output_details": {
    "contract_id": {
      "type": "string",
      "description": "Unique contract identifier"
    },
    "contract_sections": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Contract sections and terms"
    },
    "signature_required": {
      "type": "boolean",
      "description": "Whether client signature is required"
    }
  }
}