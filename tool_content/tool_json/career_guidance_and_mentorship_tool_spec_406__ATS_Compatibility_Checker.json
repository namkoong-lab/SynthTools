{
  "tool_name": "ATS Compatibility Checker",
  "tool_description": "Analyzes resume format and content for Applicant Tracking System compatibility and provides optimization recommendations.",
  "parameters": {
    "resume_text": {
      "type": "string",
      "required": true,
      "description": "Complete resume content to check for ATS compatibility"
    },
    "file_format": {
      "type": "string",
      "required": false,
      "description": "Resume file format (pdf, docx, txt)",
      "default": "docx"
    },
    "target_industry": {
      "type": "string",
      "required": false,
      "description": "Industry sector for specific ATS requirements",
      "default": "None"
    }
  },
  "error_messages": [
    "Insufficient content: Resume must contain substantial text content for meaningful ATS analysis.",
    "Unsupported format: Use one of the supported formats [pdf, docx, txt].",
    "Formatting issues detected: Resume contains elements that may not parse correctly in ATS systems."
  ],
  "usage": "Submit resume content and format details to receive comprehensive ATS compatibility assessment and specific optimization recommendations.",
  "output_details": {
    "ats_score": {
      "type": "number",
      "description": "Overall ATS compatibility score from 0-100"
    },
    "format_issues": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Specific formatting problems that may cause ATS parsing issues"
    },
    "optimization_recommendations": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Actionable steps to improve ATS compatibility"
    },
    "section_readability": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Assessment of how well each resume section will be parsed by ATS"
    }
  }
}