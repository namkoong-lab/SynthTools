{
  "tool_name": "Schedule Customizer",
  "tool_description": "Personalizes cleaning schedules by adjusting timing, frequency, and task selection based on individual preferences and constraints.",
  "parameters": {
    "base_template_id": {
      "type": "string",
      "required": true,
      "description": "Starting template to customize"
    },
    "customization_name": {
      "type": "string",
      "required": true,
      "description": "Name for the customized schedule"
    },
    "frequency_adjustments": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "required": false,
      "description": "Task-frequency modifications in format 'task_id:new_frequency'",
      "default": null
    },
    "excluded_tasks": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "required": false,
      "description": "Task IDs to remove from schedule",
      "default": null
    },
    "added_tasks": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "required": false,
      "description": "Additional task IDs to include",
      "default": null
    },
    "time_shifts": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "required": false,
      "description": "Time adjustments in format 'task_id:new_time'",
      "default": null
    },
    "personal_notes": {
      "type": "string",
      "required": false,
      "description": "Custom notes or reminders",
      "default": null
    }
  },
  "error_messages": [
    "Invalid base_template_id: Template does not exist",
    "Invalid frequency_adjustments format: Use 'task_id:frequency' format",
    "Invalid excluded_tasks: One or more task IDs don't exist in template",
    "Invalid added_tasks: One or more task IDs don't exist",
    "Invalid time_shifts format: Use 'task_id:time' format with valid times"
  ],
  "usage": "Take a base template and modify it to match your specific needs and preferences. This creates your final personalized schedule.",
  "output_details": {
    "customized_schedule_id": {
      "type": "string",
      "description": "Unique ID for the customized schedule"
    },
    "changes_summary": {
      "type": "string",
      "description": "Summary of modifications made"
    },
    "validation_status": {
      "type": "string",
      "description": "Whether customization is valid (valid, warning, error)"
    },
    "updated_time_estimate": {
      "type": "number",
      "description": "New weekly time commitment in hours"
    }
  }
}