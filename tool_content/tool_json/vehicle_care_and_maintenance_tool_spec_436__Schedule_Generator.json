{
  "tool_name": "Schedule Generator",
  "tool_description": "Generates comprehensive maintenance schedules by combining vehicle profiles, parsed rules, and usage patterns into actionable timelines.",
  "parameters": {
    "vehicle_profile_id": {
      "type": "string",
      "required": true,
      "description": "Vehicle profile identifier"
    },
    "parsed_rules": {
      "type": "array",
      "required": true,
      "description": "Structured maintenance rules from Rule Parser",
      "items": {
        "type": "string"
      },
      "minItems": 1,
      "maxItems": 50
    },
    "usage_analysis": {
      "type": "string",
      "required": true,
      "description": "Usage pattern analysis results in format 'category:multiplier:avg_monthly_miles'"
    },
    "current_date": {
      "type": "string",
      "required": true,
      "description": "Current date for schedule calculation",
      "format": "date-time"
    },
    "schedule_horizon_months": {
      "type": "integer",
      "required": false,
      "description": "How many months ahead to schedule (6-60)",
      "default": 12
    },
    "include_seasonal": {
      "type": "boolean",
      "required": false,
      "description": "Include seasonal maintenance items",
      "default": true
    }
  },
  "error_messages": [
    "Invalid profile reference: Vehicle profile ID not found.",
    "Malformed rules: Unable to process maintenance rules format.",
    "Invalid usage analysis format: Use 'category:multiplier:avg_monthly_miles' format.",
    "Invalid schedule horizon: Must be between 6 and 60 months.",
    "Insufficient rule data: Need at least one valid maintenance rule to generate schedule."
  ],
  "usage": "Combine vehicle profile, maintenance rules, and usage analysis to generate a complete maintenance schedule. Specify horizon and seasonal preferences.",
  "output_details": {
    "schedule_id": {
      "type": "string",
      "description": "Generated unique schedule identifier"
    },
    "maintenance_items": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Scheduled maintenance items in format 'service:due_date:due_mileage:priority'"
    },
    "total_scheduled_items": {
      "type": "integer",
      "description": "Total number of scheduled maintenance items"
    },
    "next_service_date": {
      "type": "string",
      "description": "Date of next scheduled maintenance",
      "format": "date-time"
    }
  }
}