{
  "Tool Call": {
    "column_metadata": [
      "user_id INT PRIMARY KEY",
      "username VARCHAR(50) NOT NULL",
      "email VARCHAR(255) UNIQUE",
      "created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP",
      "balance DECIMAL(10,2)",
      "is_active BOOLEAN DEFAULT TRUE"
    ],
    "target_database_type": "postgresql"
  },
  "Return Data": {
    "compatible_types": 4,
    "incompatible_types": 2,
    "type_recommendations": [
      "Convert TIMESTAMP to TIMESTAMPTZ for timezone awareness",
      "Consider using NUMERIC instead of DECIMAL for better PostgreSQL compatibility",
      "VARCHAR types are fully compatible with PostgreSQL"
    ],
    "migration_warnings": [
      "TIMESTAMP without timezone may cause issues in multi-timezone applications",
      "DECIMAL precision may be interpreted differently - verify numeric precision requirements"
    ]
  }
}