[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
markers = [
    "integration: slow live-vLLM tests (run with `pytest -m integration`)",
]
addopts = "-ra --strict-markers"
